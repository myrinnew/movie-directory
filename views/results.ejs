<h1>Results Page</h1>
<ul>
    <% data.forEach(movie => { %>
        <li>
            <img src="<%= movie.Poster %>">
            <br>
            <strong><%= movie.Title %></strong> - <%= movie.Year %></li>
            <br><br>
    <% }); %>
</ul>

<a href="/"><button>Do Another Search</button></a>